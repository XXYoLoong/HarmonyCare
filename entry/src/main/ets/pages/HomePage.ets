@Entry
@Component
struct HomePage {
  @State message: string = '我是首页'
  @State array: Array<ExerciseplanEntity> = [
    new ExerciseplanEntity("跑步", "长跑、中长跑、短跑"),
    new ExerciseplanEntity("跑步", "长跑、中长跑、短跑"),
    new ExerciseplanEntity("跑步", "长跑、中长跑、短跑"),
    new ExerciseplanEntity("跑步", "长跑、中长跑、短跑")
  ];
  /**
   * In low-code mode, do not add anything to the build function, as it will be
   * overwritten by the content generated by the .visual file in the build phase.
   */
  build() {
 
  }
}
export  class  ExerciseplanEntity{
  public name: string;
  public msg: string;

  constructor(name: string, msg: string) {
  this.name = name;
  this.msg = msg;
  this.name = name;
  this.msg = msg;
}
}